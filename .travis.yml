language: node_js

node_js:
  - 'node'
  - 8
  - 7
  - 6

install:
  - npm i -g npm@latest
  - npm install

before_install:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

cache:
  directories:
    - node_modules

after_success: codecov
