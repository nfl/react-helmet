language: node_js

node_js:
  - 'node'
  - 6
  - 5
  - 4

before_install:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - npm install -g npm

script:
  - npm run lint
  - npm test -- --browsers PhantomJS

cache:
  directories:
    - node_modules
